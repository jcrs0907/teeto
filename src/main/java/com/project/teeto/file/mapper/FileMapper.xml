<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.teeto.file.mapper.FileMapper">

    <insert id="insert"
            parameterType="teetoFile"
            useGeneratedKeys="true" keyColumn="FILE_SEQNO" keyProperty="seqno">
        INSERT INTO TB_FILE_M (
            FILE_SEQNO
           ,FILE_GRP_CD
           ,FILE_NM
           ,FILE_EXT
           ,FILE_PATH
           ,FILE_SIZE
           ,CRE_DTTM
        ) VALUES (
            TB_FILE_M_SEQ.nextval
          , #{groupCd}
          , #{fileNm}
          , #{extension}
          , #{path}
          , #{size}
          , SYSDATE)
    </insert>
</mapper>